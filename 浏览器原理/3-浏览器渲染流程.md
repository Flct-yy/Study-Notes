三、浏览器渲染流程（重点中的重点）

你一定要能完整说出这条链路 👇

标准答案结构

HTML → DOM Tree

CSS → CSSOM Tree

DOM + CSSOM → Render Tree

Layout（回流 / 重排）

Paint（重绘）

Composite（合成）

高频追问

DOM 和 Render Tree 区别？

display:none 和 visibility:hidden 区别？

哪些操作会触发回流？