## Web三大基石
HTML Web页面的结果骨架
CSS 页面视觉表现和布局
JavaScript 脚本语言 实现页面动态互动

## HTML是什么
HTML 指的是 超文本标记语言
HTML 不是编程语言 是一种标记语言
标记语言 是一套 标记标签
HTML 使用标记标签来描述页面

## HTML4 和 HTML5区别
| 比较维度     | HTML5                                     | HTML4                                                                                   |
| ------------ | ----------------------------------------- | --------------------------------------------------------------------------------------- |
| 文档声明     | 复杂的DOCTYPE声明，基于SGML               | 简化的DOCTYPE声明:`<IDOCTYPE html>`                                                     |
| 语义化标签   | 语义化标签较少，主要依赖`<div>`和`<span>` | 新增语义化标签，如`<article>`,`<section>`,`<header>`,`<footer>`,`<nav>`等               |
| 多媒体支持   | 需要插件支持(如Flash)                     | 原生支持`<audio>`和`<video>`标签，无需插件                                              |
| 图形支持     | 依赖外部插件或技术(如Flash、SVG)          | 原生支持`<canvas>`和增强的SVG功能                                                       |
| 表单元素     | 基本表单元素，功能有限                    | 新增表单输入类型,如`<input type="email">`,`<input type="date">`,`<inputtype="range">`等 |
| 离线存储     | 依赖浏览器缓存或外部技术                  | 支持localStorage和sessionStorage,以及IndexedDB用于离线存储                              |
| 网络通信     | 依赖XMLHttpRequest进行异步通信            | 引入WebSocket和Server-Sent Events,支持实时通信                                          |
| 浏览器支持   | 现代浏览器支持有限，需考虑兼容性          | 广泛支持，现代浏览器全面支持                                                            |
| 性能优化     | 依赖开发者手动优化                        | 提供`<link rel="preload">`和`<link rel="prefetch">`等性能优化功能                       |
| 移动设备支持 | 需额外配置，移动支持有限                  | 原生支持移动设备优化，如`<meta name="viewport">`                                        |


## HTML基本结构
```html
<!-- 声明为HTML5文档 -->
<!DOCTYPE html>
<!-- 根元素 -->
<html lang="en"> 
  <!-- 包含文档元数据 -->
  <head>
    <!-- 定义网页编码格式为UTF-8 -->
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- 网页标题 -->
    <title>Document</title>
  </head>
  <body>
    <!-- 网页内容 -->
  </body>
</html>
```

## HTML标签
HTML标签是HTML文档的基本结构
HTML标签由尖括号包围的关键词
通常是承兑出现的
第一个标签是开始标签 第二个标签是结束标签
开始和结束标签也被称做为开放标签和闭合标签
HTMl元素是从开始标签到结束标签的所有代码
大部分的HTM元素可以嵌套
HTML文档由嵌套的HTML构成的
没用内容的元素被称为空元素 空元素是在开始标签中关闭的
## HTML属性
元素属性一般是在开始的标签中 以键值对表示的

## HTML常见的元素

| 元素类型 | 含义                                                             | 常见元素                                                         | 使用场景                                             |
| -------- | ---------------------------------------------------------------- | ---------------------------------------------------------------- | ---------------------------------------------------- |
| 块级元素 | 占据整行的元素，前后自动换行，可包含其他元素                     | `<div>`, `<p>`, `<header>`, `<footer>`, `<section>`, `<article>` | 用于构建页面布局和结构，适合容纳其他元素             |
| 行内元素 | 仅占据内容所需宽度的元素，不自动换行，通常只能包含文本或行内元素 | `<span>`, `<a>`, `<em>`, `<strong>`, `<img>`                     | 用于修饰文本或嵌入小型内容，不打断文本流动           |
| 列表元素 | 用于创建项目列表，有序或无序                                     | `<ul>`, `<ol>`, `<li>`                                           | 提供结构化方式展示项目集合，常用于导航菜单或项目清单 |
| 表格元素 | 用于显示二维数据表格，由行和列组成                               | `<table>`, `<tr>`, `<td>`, `<th>`                                | 用于展示结构化数据，适合数据密集型内容展示           |
| 分割元素 | 用于创建视觉分隔，分隔内容块                                     | `<hr>`                                                           | 用于视觉分隔，不影响文档结构                         |

## 常见块级HTML元素说明
| 元素名      | 含义           | 常见使用场景       | 示例代码                     | 注意事项                                     |
| ----------- | -------------- | ------------------ | ---------------------------- | -------------------------------------------- |
| `<div>`     | 通用块级容器   | 布局、分组内容     | `<div>Content</div>`         | 无语义，需配合CSS使用                        |
| `<p>`       | 段落           | 文本段落           | `<p>Paragraph</p>`           | 自动添加上下间距                             |
| `<header>`  | 页眉或章节头部 | 页面或文章的头部   | `<header>Header</header>`    | 语义化标签，增强可读性                       |
| `<footer>`  | 页脚或章节底部 | 页面或文章的底部   | `<footer>Footer</footer>`    | 语义化标签，增强可读性                       |
| `<section>` | 文档中的章节   | 内容分组           | `<section>Section</section>` | 需包含标题以增强语义                         |
| `<article>` | 独立内容块     | 博客文章、新闻报道 | `<article>Article</article>` | 适合自包含的内容（可独立于页面其他部分存在） |

---

#### 补充说明
- **语义化标签**（如`<header>`、`<footer>`、`<section>`、`<article>`）的核心作用是让代码结构更清晰，同时提升页面的可访问性和SEO表现。
- `<div>`作为无语义的通用容器，适合用于纯粹的样式和布局控制，不承载内容含义。
- `<p>`标签会自动生成上下外边距，因此不要用它来做纯布局的空白占位。


| 元素名     | 含义         | 常见使用场景         | 示例代码                            | 注意事项                      |
| ---------- | ------------ | -------------------- | ----------------------------------- | ----------------------------- |
| `<span>`   | 通用行内容器 | 文本样式、分组       | `<span>Text</span>`                 | 无语义，需配合CSS使用         |
| `<a>`      | 超链接       | 链接到其他页面或资源 | `<a href="#">Link</a>`              | 需设置`href`属性              |
| `<em>`     | 强调文本     | 强调某部分文本       | `<em>Emphasized</em>`               | 默认斜体显示                  |
| `<strong>` | 重要文本     | 强调重要内容         | `<strong>Strong</strong>`           | 默认加粗显示                  |
| `<img>`    | 图像         | 嵌入图片             | `<img src="image.jpg" alt="Image">` | 需设置`alt`属性以增强可访问性 |

---

### 补充说明
- `<span>`是行内版的`<div>`，仅用于样式和文本分组，不具备语义。
- `<a>`标签的`href`属性不仅可以链接到网页，也可以是邮箱（`mailto:`）、电话（`tel:`）或锚点（`#id`）。
- `<em>`和`<strong>`是语义化标签，分别表示语气强调和内容重要性，不要仅为了斜体/加粗效果而使用它们，纯样式需求建议用CSS。
- `<img>`的`alt`属性是必须的，它在图片加载失败时显示，也能被屏幕阅读器识别，提升网站的可访问性

| 元素名 | 含义     | 常见使用场景               | 示例代码                                    | 注意事项                                 |
| ------ | -------- | -------------------------- | ------------------------------------------- | ---------------------------------------- |
| `<ul>` | 无序列表 | 项目列表（无顺序要求）     | `<ul><li>Item</li></ul>`                    | 列表项需使用`<li>`标签                   |
| `<ol>` | 有序列表 | 排序项目列表（有先后顺序） | `<ol><li>Item</li></ol>`                    | 列表项需使用`<li>`标签                   |
| `<li>` | 列表项   | 列表中的单个项目           | `<li>List Item</li>`                        | 需嵌套在`<ul>`或`<ol>`中                 |
| `<dl>` | 定义列表 | 术语及其定义的配对展示     | `<dl><dt>Term</dt><dd>Definition</dd></dl>` | 需配合`<dt>`（术语）和`<dd>`（定义）使用 |

---

### 补充说明
- `<ul>`默认会为列表项添加圆点符号，`<ol>`默认会添加数字序号，这些样式都可以通过CSS自定义。
- `<li>`是唯一合法的列表子元素，不能直接在`<ul>`或`<ol>`中放置其他标签。
- `<dl>`常用于展示术语表、FAQ等需要“名词+解释”形式的内容，语义性更强。

你希望把两张图里的HTML标签合并整理成一个清晰的Markdown表格，我已经帮你分类汇总（区分**表格相关标签**和**文本样式相关标签**），方便查阅和记忆：

| 标签       | 类别                 | 功能描述                                              | 补充说明                                                                           |
| :--------- | :------------------- | :---------------------------------------------------- | :--------------------------------------------------------------------------------- |
| `<table>`  | 表格构建标签         | 定义整个表格的容器，是表格的根元素                    | 必须嵌套`<tr>`才能形成完整表格，负责承载所有表格内容                               |
| `<tr>`     | 表格构建标签         | 定义表格中的一行（table row）                         | 仅能嵌套在`<table>`内，内部需包含`<td>`或`<th>`才能形成可显示的单元格              |
| `<td>`     | 表格构建标签         | 定义表格中的普通数据单元格（table data）              | 嵌套在`<tr>`内，用于展示表格的核心数据内容，无默认特殊样式                         |
| `<th>`     | 表格构建标签         | 定义表格的表头单元格（table header）                  | 嵌套在`<tr>`内，默认视觉效果为**加粗+居中**，用于描述列/行的含义，提升表格可读性   |
| `<b>`      | 文本样式标签         | 将包裹的文本进行加粗显示                              | 仅具备视觉样式，无任何语义强调，不推荐在现代语义化开发中使用，建议替换为`<strong>` |
| `<i>`      | 文本样式标签         | 将包裹的文本进行斜体显示                              | 仅具备视觉样式，无任何语义强调，现代开发中建议替换为`<em>`（表达语气强调）         |
| `<strong>` | 文本样式标签（推荐） | 将文本加粗显示，同时表达“内容具有重要性”的语义        | 视觉效果与`<b>`一致，但具备语义化属性，对SEO、屏幕阅读器更友好，优先使用           |
| `<em>`     | 文本样式标签（推荐） | 将文本斜体显示，同时表达“内容需要语气/逻辑强调”的语义 | 视觉效果与`<i>`一致，具备语义化属性，替代`<i>`完成强调类斜体需求                   |

## 注释
`<!-- HTML注释 -->` 插入HTML代码中，不会对代码产生任何影响，仅供阅读者参考。

## <a> 链接
`<a href="URL">Link text</a>`
href属性规定链接的目标
target 属性

| 属性值      | 描述                                           | 适用场景                                                             |
| :---------- | :--------------------------------------------- | :------------------------------------------------------------------- |
| `_blank`    | 在新窗口/新标签页中打开被链接文档              | 希望用户在浏览外部链接时，不离开当前页面（如导航栏的友情链接）       |
| `_self`     | 默认值，在相同的框架或标签页中打开被链接文档   | 网站内部页面跳转，保持用户在当前标签页内浏览                         |
| `_parent`   | 在父框架集中打开被链接文档                     | 适用于框架（Frame）结构页面，在父级框架中加载内容                    |
| `_top`      | 在整个窗口中打开被链接文档，会清除所有框架结构 | 用于突破多层框架嵌套，让链接内容占满整个浏览器窗口                   |
| `framename` | 在指定名称的框架中打开被链接文档               | 多框架页面中，定向在某个框架内加载内容（需提前为框架设置`name`属性） |

## 图像
`<img src="image.jpg" alt="Image description">`
src属性规定图像的URL
alt属性规定图像的替代文本，当图像不能显示时，显示该文本


## HTML语义化

| 核心优势                 | 具体作用                                                                         | 实际价值                                           |
| :----------------------- | :------------------------------------------------------------------------------- | :------------------------------------------------- |
| **提高可读性和可维护性** | 代码结构清晰，便于开发者快速识别和修改页面模块，提升开发与协作效率。             | 降低后期维护成本，多人协作时能更快理解代码意图。   |
| **增强可访问性**         | 帮助屏幕阅读器等辅助技术准确解析页面内容，为视障等特殊用户提供更友好的浏览体验。 | 让网站覆盖更多用户群体，符合无障碍设计的行业标准。 |
| **有利于SEO优化**        | 使搜索引擎更精准地理解网页内容与结构，提升内容的索引效率。                       | 可能提高关键词排名，增加自然流量，提升网站曝光度。 |
| **提供更好的用户体验**   | 页面内容结构化呈现，导航更清晰，用户可快速定位信息。                             | 降低用户的信息获取成本，提升留存率与交互意愿。     |

AOM 自行查看
不使用语义化的
```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <title>非语义化HTML示例</title>
</head>
<body>
    <div>
        <span>Three words</span>
        <div>
            <a>one word</a>
            <a>one word</a>
            <a>one word</a>
            <a>one word</a>
        </div>
    </div>

    <div>
        <div>
            <div>five words</div>
        </div>
        <div>
            <div>three words</div>
            <div>forty-six words</div>
            <div>forty-four words</div>
        </div>
        <div>
            <div>seven words</div>
            <div>sixty-eight words</div>
            <div>forty-four words</div>
        </div>
    </div>

    <div>
        <span>five words</span>
    </div>
</body>
</html>
```
使用语义化的
```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <title>语义化HTML示例</title>
</head>
<body>
    <header>
        <h1>Three words</h1>
        <nav>
            <a>one word</a>
            <a>one word</a>
            <a>one word</a>
            <a>one word</a>
        </nav>
    </header>

    <main>
        <header>
            <h1>five words</h1>
        </header>
        <section>
            <h2>three words</h2>
            <p>forty-six words</p>
            <p>forty-four words</p>
        </section>
        <section>
            <h2>seven words</h2>
            <p>sixty-eight words</p>
            <p>forty-four words</p>
        </section>
    </main>

    <footer>
        <p>five words</p>
    </footer>
</body>
</html>
```

F12打开开发者工具 选择 元素 tab 点击右上角按钮或点击无障碍功能卡片 查看AOM无障碍功能树


## 语义化标签
| 类型             | 标签名         | 介绍                                                               | 核心使用场景                                                     |
| :--------------- | :------------- | :----------------------------------------------------------------- | :--------------------------------------------------------------- |
| **结构性标签**   | `<header>`     | 表示页面或章节的头部，通常包含标题、导航链接等。                   | 页面顶部的网站标题+导航栏，或文章/章节的标题区域。               |
| **结构性标签**   | `<nav>`        | 用于定义导航链接的集合，通常用于页面的主导航部分。                 | 主导航菜单、侧边导航、面包屑导航等可跳转的链接集合。             |
| **结构性标签**   | `<main>`       | 表示文档的主要内容区域，页面中只能有一个`<main>`元素。             | 页面的核心内容主体，不包含页头、页脚、侧边栏等重复内容。         |
| **结构性标签**   | `<section>`    | 表示文档中的章节，用于将内容分组，通常包含标题。                   | 文章的不同章节、产品展示的不同模块、页面的功能区块。             |
| **结构性标签**   | `<article>`    | 表示独立的内容块，如博客文章、新闻报道等，适合自包含的内容。       | 博客文章、论坛帖子、新闻详情等可独立传播的完整内容。             |
| **结构性标签**   | `<aside>`      | 表示与页面主要内容间接相关的内容，如侧边栏、广告、引用等。         | 侧边栏的推荐文章、作者信息、广告模块、相关链接等。               |
| **结构性标签**   | `<footer>`     | 表示页面或章节的页脚，通常包含版权信息、联系信息等。               | 页面底部的版权声明、联系方式、友情链接，或文章的作者信息。       |
| **内容描述标签** | `<figure>`     | 用于封装图像、图表、照片等独立内容，通常与`<figcaption>`一起使用。 | 图文组合的展示，如文章中的配图+说明、数据图表+标题。             |
| **内容描述标签** | `<figcaption>` | 为`<figure>`元素提供标题或说明，通常用于描述图像或图表。           | 为`<figure>`内的图片/图表添加标题或说明文字。                    |
| **内容描述标签** | `<mark>`       | 用于标记文本的高亮部分，表示与上下文相关的重要性。                 | 搜索结果中的关键词高亮、文章中需要重点提醒的文本。               |
| **内容描述标签** | `<time>`       | 用于表示时间或日期，支持机器可读格式。                             | 文章发布时间、活动日期、事件时间等，便于搜索引擎和辅助技术识别。 |
| **交互性标签**   | `<details>`    | 用于描述可展开和折叠的内容，通常与`<summary>`标签配合使用。        | 常见问题（FAQ）、折叠面板、可展开的额外说明内容。                |
| **交互性标签**   | `<summary>`    | 为`<details>`元素提供可见的标题，用户可以点击以查看详细内容。      | 作为`<details>`的标题，点击后展开/折叠隐藏内容。                 |

### 语义化结构
![alt text](image.png)

## 表单交互
### 表单核心作用与结构解析
这张图清晰说明了HTML表单的核心价值与基础结构，我帮你梳理成结构化内容，便于理解和应用：

#### 表单的核心作用
- **数据收集**：作为用户输入文本、选择选项、上传文件等操作的交互入口。
- **数据提交**：将用户输入的信息发送至服务器，常见场景包括登录、注册、搜索等。
- **数据验证**：通过前端实时校验（如格式检查、必填项校验），提升数据准确性，减少无效请求。

---

#### 表单基本结构代码
你可以直接复制这段完整可运行的代码：
```html
<form action="/submit" method="POST" enctype="multipart/form-data">
    <!-- 输入控件 -->
    <label for="email">邮箱：</label>
    <input type="email" id="email" name="email" required>
    <!-- 提交按钮 -->
    <button type="submit">提交</button>
</form>
```
### `<form>` 元素核心属性解析表

| 属性             | 描述                                                                             | 示例值                          | 默认值                              | 兼容性说明                                          | 核心使用场景                                                                        |
| :--------------- | :------------------------------------------------------------------------------- | :------------------------------ | :---------------------------------- | :-------------------------------------------------- | :---------------------------------------------------------------------------------- |
| `action`         | 指定表单提交的服务器端URL                                                        | `action="/submit"`              | 当前页面URL                         | 所有浏览器支持                                      | 定义表单数据的接收地址，如用户提交的登录信息会发送到该URL处理                       |
| `method`         | 定义数据提交的HTTP方法（GET或POST）                                              | `method="POST"`                 | `GET`                               | 所有浏览器支持                                      | `GET`用于查询类操作（如搜索），`POST`用于提交敏感/大量数据（如注册、支付）          |
| `enctype`        | 编码类型，控制表单数据在发送前的编码方式                                         | `enctype="multipart/form-data"` | `application/x-www-form-urlencoded` | 所有浏览器支持，文件上传需设为`multipart/form-data` | 包含文件上传时必须使用`multipart/form-data`，否则文件无法正确提交                   |
| `autocomplete`   | 控制表单的自动填充功能（on或off）                                                | `autocomplete="off"`            | `on`                                | HTML5新增，主流浏览器支持                           | 敏感字段（如验证码、一次性密码）建议关闭自动填充                                    |
| `novalidate`     | 提交表单时跳过HTML5原生验证                                                      | `novalidate`                    | -                                   | HTML5新增，现代浏览器支持                           | 需要自定义前端验证逻辑时，可关闭原生验证                                            |
| `target`         | 定义服务器响应显示的位置（如`_blank`、`_self`、`_parent`、`_top`或自定义框架名） | `target="_blank"`               | `_self`                             | 所有浏览器支持                                      | 希望响应在新标签页打开时使用`_blank`，保持当前页面不跳转                            |
| `name`           | 表单名称，用于JavaScript或后端引用                                               | `name="loginForm"`              | -                                   | 所有浏览器支持                                      | 便于在脚本中精准定位表单，如`document.forms.loginForm`                              |
| `rel`            | 定义与目标URL的关系（如`nofollow`、`opener`）                                    | `rel="nofollow"`                | -                                   | HTML5新增，部分浏览器支持                           | 提交到外部链接时使用`nofollow`，避免搜索引擎权重传递                                |
| `accept-charset` | 指定表单提交时使用的字符编码（如UTF-8）                                          | `accept-charset="UTF-8"`        | 页面编码                            | 所有浏览器支持，但通常由页面编码自动处理            | 确保表单数据以正确编码提交，避免乱码问题                                            |
| `data-*`         | 自定义数据属性，用于存储与表单相关的额外信息                                     | `data-analytics-id="123"`       | -                                   | 所有浏览器支持                                      | 可用于埋点、业务标识等自定义场景，如通过JavaScript读取`dataset.analyticsId`获取信息 |



### 表单元素完整解析表
| 元素标签     | 用途             | 关键属性                                  | 示例                                      | 补充说明                                                                                                            |
| :----------- | :--------------- | :---------------------------------------- | :---------------------------------------- | :------------------------------------------------------------------------------------------------------------------ |
| `<form>`     | 定义表单容器     | `action`, `method`, `enctype`             | `<form action="/submit" method="POST">`   | `action`指定提交地址；`method`用`POST`提交敏感数据，`GET`用于查询；`enctype`在上传文件时需设为`multipart/form-data` |
| `<input>`    | 通用输入控件     | `type`, `name`, `placeholder`, `required` | `<input type="text" name="username">`     | `type`支持`text`/`password`/`email`/`radio`/`checkbox`等多种类型；`name`是提交到服务器的字段名                      |
| `<textarea>` | 多行文本输入     | `rows`, `cols`, `maxlength`               | `<textarea rows="4"></textarea>`          | `rows`和`cols`控制初始显示尺寸，`maxlength`限制输入字符数，适合输入长文本（如备注、留言）                           |
| `<select>`   | 下拉选择框       | `multiple`, `size`                        | `<select><option>选项1</option></select>` | `multiple`允许多选；`size`控制可见选项数量；需配合`<option>`定义选项                                                |
| `<button>`   | 交互按钮         | `type`（`submit`/`reset`/`button`）       | `<button type="submit">提交</button>`     | `type="submit"`用于提交表单，`reset`用于重置表单，`button`仅作为普通按钮                                            |
| `<label>`    | 关联输入项的标签 | `for`（绑定输入项id）                     | `<label for="email">邮箱：</label>`       | `for`属性值需与对应输入框的`id`一致，点击标签可聚焦输入框，提升可访问性                                             |


### 表单核心属性解析表
| 属性           | 适用元素                                | 作用                             | 示例                                 | 补充说明                                                                                     |
| :------------- | :-------------------------------------- | :------------------------------- | :----------------------------------- | :------------------------------------------------------------------------------------------- |
| `action`       | `<form>`                                | 指定表单提交的目标URL            | `action="/submit-data"`              | 表单数据会被发送到该地址进行处理；若留空则提交到当前页面                                     |
| `method`       | `<form>`                                | 定义表单提交方式（`GET`/`POST`） | `method="POST"`                      | `GET`将数据拼接在URL后，适合查询类操作；`POST`将数据放在请求体中，适合提交敏感/大量数据      |
| `enctype`      | `<form>`                                | 设置表单数据的编码类型           | `enctype="multipart/form-data"`      | 默认值为`application/x-www-form-urlencoded`；仅在包含文件上传时需设置为`multipart/form-data` |
| `required`     | 输入类元素（`<input>`、`<textarea>`等） | 强制字段为必填项                 | `<input required>`                   | 浏览器会自动校验，若未填写则阻止表单提交并提示用户                                           |
| `pattern`      | 输入类元素                              | 用正则表达式验证输入内容         | `pattern="[A-Za-z]{3}"`              | 仅允许输入3个英文字母，常用于格式校验（如手机号、邮编）                                      |
| `autocomplete` | 输入类元素                              | 控制浏览器是否自动填充           | `autocomplete="off"`                 | 可设为`on`（开启）或`off`（关闭），敏感字段（如验证码）建议关闭                              |
| `disabled`     | 输入类/按钮元素                         | 禁用元素，使其无法交互           | `<button disabled>不可点击</button>` | 禁用后元素样式会变灰，且不会提交数据                                                         |
| `maxlength`    | 文本输入类元素                          | 限制输入的最大字符数             | `<input maxlength="10">`             | 适用于`<input type="text">`、`<textarea>`等，超过长度后无法继续输入                          |


### `<input>` 元素 `type` 属性完整解析表

| `type` 值  | 用途                 | HTML5新增 | 示例                                 | 补充说明                                                                                                |
| :--------- | :------------------- | :-------- | :----------------------------------- | :------------------------------------------------------------------------------------------------------ |
| `text`     | 单行文本输入         | ❌         | `<input type="text" name="name">`    | 用于输入普通文本，如用户名、姓名等                                                                      |
| `password` | 密码输入（掩码显示） | ❌         | `<input type="password">`            | 输入内容会被隐藏，用于密码、验证码等敏感信息                                                            |
| `email`    | 邮箱格式验证         | ✅         | `<input type="email" required>`      | 浏览器会自动验证输入是否为合法邮箱格式，结合`required`可强制必填                                        |
| `number`   | 数字输入（可设范围） | ✅         | `<input type="number" min="1">`      | 仅允许输入数字，支持`min`/`max`/`step`属性控制范围和步长                                                |
| `date`     | 日期选择器           | ✅         | `<input type="date">`                | 提供原生日期选择界面，简化日期输入，不同浏览器样式略有差异                                              |
| `file`     | 文件上传             | ❌         | `<input type="file" multiple>`       | 支持单文件/多文件上传，`multiple`属性允许选择多个文件，需配合`<form enctype="multipart/form-data">`使用 |
| `checkbox` | 多选框               | ❌         | `<input type="checkbox">`            | 允许用户选择多个选项，如兴趣标签、协议勾选等                                                            |
| `radio`    | 单选框（同组互斥）   | ❌         | `<input type="radio" name="gender">` | 同组单选框需设置相同`name`值，确保互斥选择，如性别选择                                                  |
| `submit`   | 提交表单按钮         | ❌         | `<input type="submit" value="提交">` | 点击后提交所在表单，`value`属性定义按钮显示文本                                                         |

# 表单设计最佳实践

1.  **语义化结构**
    使用`<fieldset>`和`<legend>`分组表单内容：
    ```html
    <fieldset>
      <legend>用户信息</legend>
      <input type="text" name="name">
    </fieldset>
    ```

2.  **可访问性**
    使用`<label>`关联输入项，`aria-*`属性增强无障碍支持。

3.  **用户体验**
    清晰的标签、合理的输入提示（placeholder）、错误反馈、响应式布局适配多设备等。

4.  **安全防护**
    CSRF Token、输入过滤、HTTPS提交。

5.  **性能优化**
    减少不必要的字段，分步提交（多步骤表单）。

## 表单内动作可触发的事件
（可应用到几乎所有 HTML 元素，但最常用在 `<form>` 元素中）

| 属性            | 值       | HTML5新增 | 描述                                                         |
| :-------------- | :------- | :-------- | :----------------------------------------------------------- |
| `onblur`        | `script` | ❌         | 元素失去焦点时触发。                                         |
| `onchange`      | `script` | ❌         | 元素值被更改时触发（通常用于表单字段内容变化后）。           |
| `oncontextmenu` | `script` | ✅         | 当用户触发上下文菜单（如右键点击）时触发。                   |
| `onfocus`       | `script` | ❌         | 元素获得焦点时触发。                                         |
| `onformchange`  | `script` | ✅         | 表单内容发生改变时触发（非标准属性，部分浏览器可能不支持）。 |
| `onforminput`   | `script` | ✅         | 表单获得用户输入时触发（非标准属性，部分浏览器可能不支持）。 |
| `oninput`       | `script` | ✅         | 元素获取用户输入时实时触发（如文本框输入时）。               |
| `oninvalid`     | `script` | ✅         | 当表单元素验证失败时触发（例如 `required` 字段为空时）。     |
| `onreset`       | `script` | ❌         | 表单重置按钮被点击时触发（HTML5 中已废弃）。                 |
| `onselect`      | `script` | ❌         | 元素内文本被选中时触发（如文本框中选择文字）。               |
| `onsubmit`      | `script` | ❌         | 表单提交时触发（通常用于验证表单数据）。                     |

# 数据驱动 VS 行为驱动的表单交互

由于React、Vue等框架的流行，表单数据的提交几乎都脱离HTML元素，纯数据驱动：

---

## 数据驱动表单交互
（使用`<button>`元素，事件绑定在按钮上）
```javascript
const postData = reactive({
  content: '',
  isSending: false
});
const send = () => {
  // 数据 post 方法
};
```
```html
<div>
  <input v-model="postData.content">
  <button :disabled="postData.isSending" @click="send">提交</button>
</div>
```

---

## 行为驱动表单交互
（使用`<form>`元素，事件绑定在表单元素上。体验更佳，可以直接通过键盘回车键提交，支持移动端）
```javascript
const postData = reactive({
  content: '',
  isSending: false
});
const send = () => {
  // 数据 post 方法
};
```
```html
<form @submit.prevent="send">
  <input v-model="postData.content">
  <button :disabled="postData.isSending">提交</button>
</form>
```
## HTML其他特性

# SVG 核心特性与嵌入方式

## 1. SVG 基础与特性
SVG是一种基于XML的矢量图形格式，用于描述二维图形。与位图不同，SVG图形可以任意缩放而不失真。

**特性**：
- **可编程性**：SVG图形可以通过JavaScript进行动态操作和交互。
- **样式化**：支持CSS样式，可以对图形元素进行样式化。
- **动画**：支持动画效果，可以通过SMIL或JavaScript实现复杂的动画。
- **集成性**：可以嵌入到HTML文档中，与其他Web技术无缝集成。

---

## 2. SVG 嵌入网页的方式
- **直接插入**：可以直接插入网页，成为DOM的一部分，然后用JavaScript和CSS进行操作。
- **独立文件引用**：也可以写在一个独立文件中，然后用`<img>`、`<object>`、`<embed>`、`<iframe>`等标签插入网页。

## SVG 核心标签解析表
我帮你把这张图里的SVG标签整理成结构化的Markdown表格，方便你开发时快速参考：

| 标签名             | 含义                 | 使用方法                                          | 主要属性                                      |
| :----------------- | :------------------- | :------------------------------------------------ | :-------------------------------------------- |
| `<svg>`            | 定义SVG图形的容器    | 用于嵌入SVG内容，设置图形的宽度、高度和命名空间。 | `width`, `height`, `xmlns`                    |
| `<circle>`         | 绘制圆形             | 通过定义圆心坐标和半径来绘制圆形。                | `cx`, `cy`, `r`, `fill`, `stroke`             |
| `<rect>`           | 绘制矩形             | 通过定义左上角坐标、宽度和高度来绘制矩形。        | `x`, `y`, `width`, `height`, `fill`, `stroke` |
| `<ellipse>`        | 绘制椭圆             | 通过定义中心坐标和半径来绘制椭圆。                | `cx`, `cy`, `rx`, `ry`, `fill`, `stroke`      |
| `<line>`           | 绘制直线             | 通过定义起点和终点坐标来绘制直线。                | `x1`, `y1`, `x2`, `y2`, `stroke`              |
| `<polyline>`       | 绘制折线             | 通过定义一系列点来连接成折线。                    | `points`, `fill`, `stroke`                    |
| `<polygon>`        | 绘制多边形           | 通过定义一系列点来连接成闭合多边形。              | `points`, `fill`, `stroke`                    |
| `<path>`           | 绘制复杂形状         | 通过路径数据定义直线、曲线等复杂形状。            | `d`, `fill`, `stroke`                         |
| `<text>`           | 添加文本             | 通过定义文本起始位置来添加文本。                  | `x`, `y`, `fill`, `font-size`, `text-anchor`  |
| `<g>`              | 定义图形组           | 用于将多个图形元素组合在一起。                    | `transform`, `fill`, `stroke`                 |
| `<defs>`           | 定义可复用的图形元素 | 用于定义图形模板，供其他元素引用。                | N/A                                           |
| `<use>`            | 引用已定义的图形元素 | 通过引用`<defs>`中定义的图形元素。                | `xlink:href`, `x`, `y`                        |
| `<symbol>`         | 定义可复用的图形符号 | 用于定义可复用的图形，通常与`<use>`结合使用。     | `viewBox`, `preserveAspectRatio`              |
| `<clipPath>`       | 定义剪切路径         | 用于创建剪切区域，限制图形的显示范围。            | `id`                                          |
| `<linearGradient>` | 定义线性渐变         | 用于创建线性渐变效果。                            | `id`, `x1`, `y1`, `x2`, `y2`                  |
| `<radialGradient>` | 定义径向渐变         | 用于创建径向渐变效果。                            | `id`, `cx`, `cy`, `r`                         |

# Local Storage 本地存储
本地存储（Local Storage）是HTML5引入的一种Web存储机制，允许在用户的浏览器中以键值对的形式存储数据。它提供了一种简单而有效的方法来存储和检索客户端数据，具有比传统cookie更大的容量和更简单的API。

---

## 1. 持久性
- 数据存储在浏览器中，直到被明确删除，即使浏览器关闭后数据仍然存在。
- 适合存储需要在多个会话中保留的数据。

## 2. 容量
- 通常允许存储至少5MB的数据，比cookie的容量大得多。
- 不同浏览器可能对存储容量有不同的限制。

## 3. 简单API
- 通过`localStorage`对象提供简单的API进行数据存储和检索。
- 使用`setItem(key, value)`方法存储数据，`getItem(key)`方法检索数据。

## 4. 安全性
- 数据仅限于同源策略，即只能在同一域名下访问。
- 不会自动发送到服务器，减少了数据泄露的风险。

## 使用方法
# Local Storage 核心操作与场景说明

## 存储数据
```javascript
localStorage.setItem('username', 'JohnDoe');
```

## 检索数据
```javascript
const username = localStorage.getItem('username');
console.log(username); // 输出：JohnDoe
```

## 删除数据
```javascript
localStorage.removeItem('username');
```

## 清除所有数据
```javascript
localStorage.clear();
```

---

## 应用场景
- **用户设置**：保存用户的偏好设置，如主题颜色、语言选择等。
- **会话数据**：存储用户的会话信息，以便在页面刷新或重新打开时恢复状态。
- **离线应用**：支持离线Web应用，存储需要在无网络连接时访问的数据。

---

## 注意事项
- **数据类型**：本地存储只能存储字符串类型的数据，其他数据类型需要转换为字符串（如JSON）。
- **浏览器支持**：现代浏览器普遍支持本地存储，但在使用前应进行兼容性检查。
- **隐私与安全**：避免存储敏感信息，因为数据存储在客户端，可能被用户访问或修改。
